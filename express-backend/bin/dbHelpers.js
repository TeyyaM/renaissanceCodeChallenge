const generateQueryHelpers = (connection) => {

  const fetchExpensesByUserId = (inputObj) => {

  };

  const fetchUserId = (inputObj) => {

  };

  const insertExpense = (inputObj) => {

  };

  return {
    fetchExpensesByUserId,
    fetchUserId,
    insertExpense
  };
};

module.exports = generateQueryHelpers;
